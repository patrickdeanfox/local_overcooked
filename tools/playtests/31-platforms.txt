# -- Moving platforms: 2-1's seam between the trucks is deck while they touch and road while they are
#    apart (a chef on it falls), 3-3's three trucks, and 3-4's floes carrying a chef across the river -----
eval localStorage.setItem('local-overcooked.settings.v1', JSON.stringify({ version: 1, players: 2, preset: 'normal', seedMode: 'fixed', fixedSeed: 5, freePlay: true }))
eval location.reload()
wait 1500
until 25000 Phaser.Display.Canvas.CanvasPool.pool.map(e => e.parent).some(p => p && p.scene && p.scene.sys && p.scene.sys.game && p.scene.sys.game.scene.getScenes(true).some(s => s.scene.key === 'Title'))
wait 600
eval (0, eval)(await (await fetch('/tools/playtests/qa-lib.txt')).text())
eval (__ui.scene('Title').scene.start('Game', { levelId: 'oc1-2-1', seed: 7 }), 'started')
until 20000 typeof __oc !== 'undefined' && __oc.level && __oc.level.id === 'oc1-2-1'
wait 800
eval JSON.stringify(['seam', __oc.sim.getState().gates])
shot 31-00-trucks-touching.png
until 15000 __oc.sim.getState().gates[0].open === false
wait 300
eval JSON.stringify(['seam', __oc.sim.getState().gates])
shot 31-01-trucks-apart.png

# -- 3-3: three trucks, one seam open at a time ------------------------------------------------------
eval (__ui.scene('Game').scene.start('Game', { levelId: 'oc1-3-3', seed: 7 }), 'started')
until 20000 __oc.level && __oc.level.id === 'oc1-3-3' && __oc.sim.getState().elapsed > 0.5
wait 800
eval JSON.stringify(['seams', __oc.sim.getState().gates])
shot 31-02-three-trucks.png

# -- 3-4: the floes, and a chef put on one rides it down -----------------------------------------------
eval (__ui.scene('Game').scene.start('Game', { levelId: 'oc1-3-4', seed: 7 }), 'started')
until 20000 __oc.level && __oc.level.id === 'oc1-3-4' && __oc.sim.getState().elapsed > 0.5
wait 800
eval JSON.stringify(['floes', __oc.sim.getState().floes])
shot 31-03-river.png
eval ((st, f) => (st.chefs[0].x = f.x + 1.5, st.chefs[0].y = f.y + 1, JSON.stringify(['on floe', f])))(__oc.sim.getState(), __oc.sim.getState().floes.find((f) => f.y > -0.5 && f.y < 4) || __oc.sim.getState().floes[0])
wait 1500
eval JSON.stringify(['chef', __oc.sim.getState().chefs[0].x, __oc.sim.getState().chefs[0].y, 'falling', __oc.sim.getState().chefs[0].respawnIn])
shot 31-04-riding.png
errors
