# -- Chefs page and audio switches: pick aprons, see them in the kitchen, mute the music from
#    the pause menu --------------------------------------------------------------------------
#
# Title rows, in cursor order: six levels, Players, Difficulty, Chefs, Settings, Controllers - so
# Chefs is index 8. The Chefs page lists Player 1, Player 2, Back.
until 25000 Phaser.Display.Canvas.CanvasPool.pool.map(e => e.parent).some(p => p && p.scene && p.scene.sys && p.scene.sys.game && p.scene.sys.game.scene.getScenes(true).some(s => s.scene.key === 'Title'))
wait 600
eval (0, eval)(await (await fetch('/tools/playtests/qa-lib.txt')).text())
eval JSON.stringify(['chefs row', __ui.options()[2], __ui.settings().chefs])

# -- Open the Chefs page, cycle player 1 twice and player 2 once -------------------------------
tap ArrowDown
wait 150
tap ArrowDown
wait 150
tap ArrowDown
wait 150
tap ArrowDown
wait 150
tap ArrowDown
wait 150
tap ArrowDown
wait 150
tap ArrowDown
wait 150
tap ArrowDown
wait 150
eval JSON.stringify(['cursor on', __ui.index(), __ui.options()[2]])
tap Enter
wait 1200
eval JSON.stringify(['page rows', __ui.menu('Chefs')])
shot 19-00-chefs-page.png
tap ArrowRight
wait 400
tap ArrowRight
wait 400
tap ArrowDown
wait 150
tap ArrowLeft
wait 900
eval JSON.stringify(['page rows after', __ui.menu('Chefs')])
eval JSON.stringify(['stored', JSON.parse(localStorage.getItem('local-overcooked.settings.v1')).chefs])
shot 19-01-chefs-picked.png

# -- Back to the title, then start 1-1: the kitchen uses the chosen aprons ---------------------
tap Escape
wait 700
eval JSON.stringify(['title row', __ui.options()[2], __ui.settings().chefs])
tap ArrowUp
wait 150
tap ArrowUp
wait 150
tap ArrowUp
wait 150
tap ArrowUp
wait 150
tap ArrowUp
wait 150
tap ArrowUp
wait 150
tap ArrowUp
wait 150
tap ArrowUp
wait 150
tap Enter
until 20000 typeof __oc !== 'undefined' && __oc.level && __oc.sim
wait 800
eval JSON.stringify(['kitchen skins', __oc.scene.kitchen.skins])
hold KeyS 300
hold ArrowDown 300
wait 400
shot 19-02-kitchen-aprons.png

# -- Pause menu: music off, sound effects stay on ----------------------------------------------
tap Escape
wait 400
eval JSON.stringify(['pause rows', __ui.menu('Game')])
tap ArrowDown
wait 150
tap ArrowDown
wait 150
tap ArrowRight
wait 300
eval JSON.stringify(['pause rows after', __ui.menu('Game')])
eval JSON.stringify(['stored audio', JSON.parse(localStorage.getItem('local-overcooked.settings.v1')).audio])
shot 19-03-pause-music-off.png
errors
